using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// döp om till cordHandler kanske

public class Sladd : MonoBehaviour
{

    /* 
    // variabler som håller koll på vilken sladd som är aktiv

    public bool[] socketStatus = new bool[] {false,false,false,false,false,false};

    public bool cord = socketStatus(0);
    public bool yellow = socketStatus(1);
    public bool red = socketStatus(2);
    public bool blue = socketStatus(3);
    public bool green = socketStatus(4);
    public bool navy = socketStatus(5);

    public void activate()
    {
// kolla vilken som är aktiv och avaktivera den
// aktivera den nya
    }

    




























   public void checkColor() 
    {
        if(Input.GetKeyDown (KeyCode.Mouse0))
        {
           ray = new Ray(transform.position, transform.forward);
            
            if(Physics.Raycast(ray, out hit, 1000))
            {
                if(hit.collider.CompareTag("YellowSocket"))
                {
                    print ("Nu tyrcker du på den gula");
                    yellow = true;
                }

                else if(hit.collider.greenSocket)
                {
                    print ("Nu tyrcker du på den ggröna");
                    green = true;
                }
            }
        }
    }
    
*/

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
    }
}
